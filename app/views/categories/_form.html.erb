<%= form_for(category) do |f| %>
  <% if category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(category.errors.count, "error") %> prohibited this category from being saved:</h2>

      <ul>
      <% category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <label>Select Category of you book</label>
      <% Book.all.each do |cat| %>
      <%= check_box_tag 'category[book_ids][]', cat.id%>
      <%= cat.name%><br>
      <%end%>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
